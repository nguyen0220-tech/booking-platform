enum SearchType {
    USERNAME,
    EMAIL
}

enum FilterUser{
    ENABLED,
    BLOCKED
}

enum RoleName{
    USER,
    PROVIDER,
    ADMIN
}

enum FacilityStatus{
    PENDING,
    APPROVED,
    REJECTED
}
